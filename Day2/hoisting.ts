// console.log(d);
// var d=10; //var is hoisted to the top of the scope and initialized with undefined

// console.log(c);
// let c=20; //let is hoisted to the top of the scope but not initialized with undefined, it is in temporal dead zone until it is initialized
