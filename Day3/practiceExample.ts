let ag=10;
let ba=20;
//let cd:number="HEllow";    //Error: Type 'string' is not assignable to type 'number'.ts(2322) because we have declared the variable c as number but we are trying to assign a string value to it.

let dc=ag+ba;
console.log(dc);
console.log( cd); //number