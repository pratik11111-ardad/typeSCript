let eq:number=10;
let bq:string="10";

//console.log(eq==bq); //true because == operator does type coercion and converts the string "20" to number 20 before comparing
//console.log(eq===bq); //false because === operator does not do type coercion and compares both value and type, here eq is number and bq is string so it returns false